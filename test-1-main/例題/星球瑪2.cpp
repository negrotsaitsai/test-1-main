#include <stdio.h>
int main(){
	int vcode;
	scanf("%d",&vcode);
	if(vcode>=0&&vcode<=4){
		printf("non-citizen");
	}
		if(vcode>=5&&vcode<=7){
		printf("non-citizen");
	}
		if(vcode>=8&&vcode<=9){
		printf("non-citizen");
	}
}
